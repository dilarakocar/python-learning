import random
import my_module

# random.randint(20, 30)
# Returns a random number between 20 and 30 (inclusive)

random_number = random.randint(1, 10)

# Prints a different number every time the program runs
print(random_number)
